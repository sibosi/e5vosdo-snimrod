{
  "private": true,
  "name": "e5vosdo-snimrod",
  "packageManager": "npm@11.0.0",
  "scripts": {
    "web-dev": "npx turbo run dev --filter=e5vosdo-web",
    "web-build": "npx turbo run build --filter=e5vosdo-web",
    "web-lint": "npx turbo run lint --filter=e5vosdo-web"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "@types/glob": "^8.1.0",
    "turbo": "^2.5.6"
  }
}
