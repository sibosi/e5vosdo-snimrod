{
  "private": true,
  "name": "e5vosdo-snimrod",
  "packageManager": "npm@11.0.0",
  "scripts": {
    "web-dev": "turbo run dev --filter=e5vosdo-web",
    "web-build": "turbo run build --filter=e5vosdo-web",
    "web-lint": "turbo run lint --filter=e5vosdo-web"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "@types/glob": "^8.1.0",
    "turbo": "^2.5.6"
  }
}
